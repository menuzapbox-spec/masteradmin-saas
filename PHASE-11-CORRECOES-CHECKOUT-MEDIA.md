# Phase 11 — Correções pontuais

- Checkout: compatibilidade com `orders.customer_reference`.
- WhatsApp: prioriza o campo `stores.whatsapp` da loja atual.
- Fotos: garante bucket `cardapio` e upload por `stores/{store_id}/products/...`.
- Produto: botão de salvar com cor própria e confirmação visual.
- Não altera a estrutura multi-loja nem os demais fluxos.
